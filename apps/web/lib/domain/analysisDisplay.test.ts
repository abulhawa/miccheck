import { describe, it } from "vitest";

describe("analysisDisplay legacy", () => {
  it("is deprecated and unused", () => {
    // Intentionally empty: legacy module retained for backward compatibility.
  });
});
